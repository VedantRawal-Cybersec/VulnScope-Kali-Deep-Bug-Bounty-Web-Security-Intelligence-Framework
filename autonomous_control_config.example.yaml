targets:
  - https://example.com
scope_policy: scope_policy.yaml
interval_minutes: 360
max_cycles: 8
max_workers: 6
enable_google_pair: false
notes: "Unified safe autonomous mission. Runs linked modules in staged parallel mode. It does not exploit, evade, dump secrets, or change remote state."
